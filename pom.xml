<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.sahelys</groupId>
	<artifactId>payBridge</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webservices</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-log4j2</artifactId>
		</dependency>

	</dependencies>

	<build>

		<!--####################################################-->
		<!-- resource filtering : Inject properties in resources (properties files, assets, ...)-->
		<!-- specify files to be filtered -->
		<resources>
			<resource> <!-- enable filtering for only the specific file 'application.properties' -->
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>application.properties</include>
					<include>log4j2.xml</include>
				</includes>
			</resource>

			<resource> <!-- disable filtering for every file inside 'src/main/resources' -->
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
				<excludes>
					<exclude>application.properties</exclude>
					<exclude>log4j2.xml</exclude>
				</excludes>
			</resource>
		</resources>

		<!-- specify filters (where filtered resources' variables come from) -->
		<filters>
			<filter>${deployment.profile.path}/config.properties</filter>
		</filters>

		<!-- END resource filtering here -->
		<!--####################################################-->

		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>



	<!--#####################################################################################-->
	<!-- DEPLOYMENT PROFILES + RESOURCE FILTERING  -->
	<!-- https://www.baeldung.com/maven-profiles -->
	<!--#####################################################################################-->
	<profiles>
		<!-- Default profile for local development -->
		<profile>
			<id>dev</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<deployment.profile.path>${basedir}/deployment-profiles/devProfiles/dev</deployment.profile.path>
			</properties>
		</profile>

		<!-- Profile for Jenkins CI/CD deployment -->
		<profile>
			<id>recette</id>
			<properties>
				<deployment.profile.path>${basedir}/deployment-profiles/devProfiles/recette</deployment.profile.path>
			</properties>
		</profile>

		<!-- Profile for local development (btchassem) -->
		<profile>
			<id>btchassem</id>
			<properties>
				<deployment.profile.path>${basedir}/deployment-profiles/devProfiles/btchassem</deployment.profile.path>
			</properties>
		</profile>

		<!-- Profile for production deployment -->
		<profile>
			<id>prod</id>
			<properties>
				<deployment.profile.path>${basedir}/deployment-profiles/ProdProfiles/prod</deployment.profile.path>
			</properties>
		</profile>
	</profiles>
	<!--#####################################################################################-->


</project>
